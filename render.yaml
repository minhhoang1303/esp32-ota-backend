services:
  - type: web
    name: server-iot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: ADMIN_USER
        value: minhhoangcdt
      - key: ADMIN_PASS
        value: changeme